spring:
  application:
    name: java
  data :
    mongodb:
      database: myjournal
      auto-index-creation: true
      uri: ${MONGODB_URI}


server:
  port: 8081
  servlet :
    context-path: /journal
